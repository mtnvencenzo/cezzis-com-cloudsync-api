apiVersion: dapr.io/v1alpha1
kind: Component

metadata:
  name: pubsub-deadletter-topic

spec:
  type: pubsub.rabbitmq
  version: v1
  metadata:
    - name: exchangeName
      value: "deadletter-topic"
    - name: exchangeKind
      value: "topic"
    - name: connectionString
      secretKeyRef:
        name: rabbitmq_connectionstring
    - name: durable
      value: "true"
    - name: deletedWhenUnused
      value: "false"
scopes:
  - cloudsync-api-dapr

auth:
  secretStore: secret-store