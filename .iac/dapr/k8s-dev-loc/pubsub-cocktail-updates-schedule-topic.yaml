apiVersion: dapr.io/v1alpha1
kind: Component

metadata:
  name: pubsub-cocktail-updates-schedule-topic

spec:
  type: pubsub.rabbitmq
  version: v1
  initTimeout: 120s
  metadata:
    - name: exchangeName
      value: "cocktail-updates-schedule-topic"
    - name: exchangeKind
      value: "topic"
    - name: connectionString
      secretKeyRef:
        name: rabbitmq_connectionstring
    - name: durable
      value: "true"
    - name: deletedWhenUnused
      value: "false"
scopes:
  - cloudsync-api-dapr

auth:
  secretStore: secret-store
